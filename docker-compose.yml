version: '3'
services:
  db:
    image: mysql
  web:
    build: web.dockerfile
    command: bundle exec rails s -p 3000 -b '0.0.0.0'
    volumes:
       - .:/app
    ports:
       - "3000:3000"
    depends_on:
       - db
